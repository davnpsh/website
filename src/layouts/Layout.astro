<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.ico" />
        <meta name="generator" content={Astro.generator} />
        <title>daru's system</title>
    </head>
    <body>
        <slot />
    </body>
</html>

<style>
    @font-face {
        font-family: "Unifont";
        src: url("/fonts/Unifont/unifont-17.0.03.otf") format("opentype");
        font-weight: normal;
        font-style: normal;
    }

    html,
    body {
        margin: 0;
        width: 100%;
        height: 100%;

        /* Font */
        font-family: "Unifont", monospace;
        font-size: 18px;
        user-select: none;
        -webkit-font-smoothing: none;
        font-smooth: never;
        image-rendering: pixelated;

        /* Navigation */
        cursor: none;
    }
</style>
